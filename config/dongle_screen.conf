# YADS (Yet Another Dongle Screen) - Prospector 用設定
# 画面の向き: Prospector ケースに合わせて y/n を実機で調整
CONFIG_DONGLE_SCREEN_HORIZONTAL=y
CONFIG_DONGLE_SCREEN_FLIPPED=n

# 自動調光: Prospector に APDS9960 光センサを載せているなら y
CONFIG_DONGLE_SCREEN_AMBIENT_LIGHT=n

# 明るさ・アイドル
CONFIG_DONGLE_SCREEN_MAX_BRIGHTNESS=80
CONFIG_DONGLE_SCREEN_MIN_BRIGHTNESS=1
CONFIG_DONGLE_SCREEN_IDLE_TIMEOUT_S=600
CONFIG_DONGLE_SCREEN_BRIGHTNESS_KEYBOARD_CONTROL=y

# 表示ウィジェット
CONFIG_DONGLE_SCREEN_WPM_ACTIVE=n
CONFIG_DONGLE_SCREEN_MODIFIER_ACTIVE=y
CONFIG_DONGLE_SCREEN_LAYER_ACTIVE=y
# Mac 1台に USB 直結のみのため出力先は常に USB 固定 → 非表示
CONFIG_DONGLE_SCREEN_OUTPUT_ACTIVE=n
CONFIG_DONGLE_SCREEN_BATTERY_ACTIVE=y
